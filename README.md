# tor-crawl-ai-detector
Web crawler that collects pages through the Tor network on a remote server and analyzes extracted text locally using a self-hosted LLM to detect AI-generated content. Designed with a clear separation between crawling and analysis for reliability and privacy.
